"""Seeds a handful of demo customers/payments for local manual testing of the dashboard.

TODO(phase-5+): implement once Customer/Payment services exist; keep this small and
deterministic (fixed seed) — it is a dev convenience, not the synthetic dataset generator
(that's scripts/generate_synthetic_dataset.py, which is the real ML/benchmark data source).
"""


def main() -> None:
    raise NotImplementedError("TODO(phase-5)")


if __name__ == "__main__":
    main()
